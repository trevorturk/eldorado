<div class="info-left">
  <span class="title"><%= link_to h(@comment.resource), @comment.resource %></span>
  <span class="detail">&laquo; <%= link_to I18n.t(:comments), polymorphic_path(@comment.resource) + '#comments' %></span>
</div>
<div class="clear"> </div>

<%= render :partial => 'comments/comment', :object => @comment %>
