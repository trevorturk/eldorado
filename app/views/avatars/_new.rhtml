<%= error_messages_for :avatar %>
<% form_for(:avatar, :url => avatars_path, :html => { :multipart => true }) do |f| -%>
	<div class="box" id="action"<% if current_action != 'new' %> style="display:none;"<% end %>>
		<div class="top">New Avatar</div>
		<p><%= f.file_field :uploaded_data %></p>
		<p>Avatars must be smaller than 50k.</p>
		<p>
			<%= submit_tag "Upload", :disable_with => "Upload" %><span> or 
			<% if current_action != 'new' %>
				<%= link_to_function 'Cancel', "$('action').toggle()" %>
			<% else %>
				<%= link_to 'Cancel', avatars_path %>
			<% end %>
		</p>
	</div>
<% end %>

<div class="info-left"<% if current_action != 'new' %> style="display:none;"<% end %>>
	<%= link_to 'Home', home_path %> &raquo; 
	<%= link_to 'Users', users_path %> &raquo; 
	<%= link_to 'Avatars', avatars_path %> &raquo; New Avatar
</div>
<div style="clear:both;"> </div>
